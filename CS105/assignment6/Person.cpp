#include <iostream>
#include <string>
#include <vector>
#include <map>

using std::map;
using std::vector;


void Person::addMatch(int flag){
   	matched = flag;
}

string Person::getFirstChoice(){
	if(ratings.front())
}
