add-to-list 'auto-mode=alist '("\\.ys\\'" . asm-mode))


int result = (x>>1)+(x>>3);

T_Min = -2^31
T_Max = 2^31 -1

%c = prints char

(** or &&) pointer to a pointer and dereference pointer to pointer

Caller vs Callee Saved Registers
	Caller:
		

	Callee